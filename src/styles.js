"use strict";

import { BOARD_SIZE } from "./constants";

export const squareDivSize = 6;
export const pieceDivSize = squareDivSize;  ///
export const boardDivSize = squareDivSize * BOARD_SIZE;

export const boardDivWidth = `${boardDivSize}rem`;
export const boardDivHeight = `${boardDivSize}rem`;
export const pieceDivWidth = `${pieceDivSize}rem`;
export const pieceDivHeight = `${pieceDivSize}rem`;
export const squareDivWidth = `${squareDivSize}rem`;
export const squareDivHeight = `${squareDivSize}rem`;

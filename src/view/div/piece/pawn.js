"use strict";

import PieceDiv from "../../div/piece";

export default class PawnDiv extends PieceDiv {
  generateMoves() {
    const moves = [];

    return moves;
  }

  static defaultProperties = {
    className: "pawn"
  };
}
